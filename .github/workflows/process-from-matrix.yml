---
name: process-from-matrix
on:
  workflow_call:
    inputs:
      name:
        description: 'Name of job'
        required: true
        type: string
jobs:
  process:
    name: Process from matrix
    runs-on: ubuntu-latest
    steps:
      - name: process
        id: process
        run: |
          echo "Naming ${{ inputs.name }}"